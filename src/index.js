import React from "react";
import ReactDOM from "react-dom/client";
import Coox from "./Coox";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Coox />
  </React.StrictMode>
);
